import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Chaville Parking App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
